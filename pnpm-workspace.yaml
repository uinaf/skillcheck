# pnpm requires an explicit decision for every dependency build script. These
# six come from the optional promptfoo devDependency. Only evals reach them;
# lint has no dependencies.
allowBuilds:
  # promptfoo transpiles and bundles config and provider modules at runtime.
  esbuild: true
  "@swc/core": true
  protobufjs: true
  # Browser automation, local ONNX embedding models, and image processing.
  # Scenarios grade text files with a remote judge and drive no browser, so
  # these are hundreds of megabytes of install for a path that is never taken.
  "@playwright/browser-chromium": false
  onnxruntime-node: false
  sharp: false
catalog:
  vite: npm:@voidzero-dev/vite-plus-core@0.2.9
  vite-plus: 0.2.9
minimumReleaseAgeExclude:
  - adm-zip@0.6.1
overrides:
  vite: "catalog:"
  # onnxruntime-node, via promptfoo > @huggingface/transformers.
  "adm-zip@<0.6.1": "^0.6.1" # GHSA-xcpc-8h2w-3j85, GHSA-vwc7-r8mq-g2x9
  # @huggingface/transformers, via promptfoo.
  "sharp@<0.35.4": "^0.35.4" # GHSA-f88m-g3jw-g9cj, GHSA-rgj7-g3m4-5g8c
peerDependencyRules:
  allowAny:
    - vite
  allowedVersions:
    vite: "*"
