---
name: mismatched
description: "Fixture skill carrying four deliberate lint errors. Use only inside this repository's tests."
category: tools
disable-model-invocation: "true"
---

# wrongname

Four errors on purpose: an unknown frontmatter key, a name that disagrees with
the directory, a quoted `disable-model-invocation`, and this
[dangling link](does-not-exist.md).
